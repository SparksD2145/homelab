---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: local-ai-pv
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 100Gi
  persistentVolumeReclaimPolicy: Retain
  local:
    path: /opt/local-ai.sparks.codes/
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - beta

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-ai-pvc
  namespace: ai
spec:
  volumeName: local-ai-pv
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  resources:
    requests:
      storage: 100Gi
