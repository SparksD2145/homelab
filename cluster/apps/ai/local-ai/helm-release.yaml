---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: local-ai
  namespace: ai
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 5.0.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
      interval: 15m
  values:
    defaultPodOptions:
      runtimeClassName: nvidia
      nodeSelector:
        kubernetes.io/hostname: beta
    controllers:
      main:
        containers:
          main:
            image:
              repository: localai/localai
              tag: "master-gpu-nvidia-cuda-13"
            env:
              TZ: "America/Chicago"
              CUDA_ERROR_LEVEL: "50"
              NVIDIA_VISIBLE_DEVICES: "all"
            resources:
              requests:
                nvidia.com/gpu: 2
              limits:
                nvidia.com/gpu: 2
    persistence:
      config:
        enabled: true
        existingClaim: local-ai-pvc
        globalMounts:
          - path: /models
    service:
      main:
        controller: main
        ports:
          http:
            port: 8080
    ingress:
      main:
        enabled: true
        className: traefik
        annotations:
          nginx.ingress.kubernetes.io/proxy-read-timeout: "240"
          hajimari.io/enable: "false"
          hajimari.io/icon: cloud-search
          hajimari.io/info: "Local AI Runner"
          hajimari.io/targetBlank: "true"
        hosts:
          - host: &host "local-ai.sparks.codes"
            paths:
              - path: /
                pathType: Prefix
                service:
                  identifier: main
                  port: http
        tls:
          - secretName: acme-crt-secret-sparks-codes
            hosts:
              - *host
